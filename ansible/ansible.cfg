[defaults]
inventory = inventories/production/hosts.yml
roles_path = roles
remote_user = exodus-admin
host_key_checking = True
retry_files_enabled = False
interpreter_python = auto_silent
timeout = 30

[privilege_escalation]
become = True
become_method = sudo
become_user = root
